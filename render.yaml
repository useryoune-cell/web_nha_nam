services:
  - type: web
    name: webnhakinh-ai-green
    env: python
    buildCommand: cd ai_backend && pip install -r requirements-api.txt
    startCommand: cd ai_backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
